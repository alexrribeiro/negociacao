export interface NegociacoesDoDia {
    montante: number;
    vezes: number;
}

// Off: se clicar com o botão direito, rename symbol e renomear uma variável, os usos dela no controller serão renomeados também